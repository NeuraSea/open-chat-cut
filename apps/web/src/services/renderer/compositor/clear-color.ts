/**
 * Project backgrounds are explicit scene nodes. The compositor itself clears
 * to transparent so PNG and ProRes 4444 can retain an alpha channel.
 */
export const SCENE_CLEAR_COLOR: [number, number, number, number] = [0, 0, 0, 0];
